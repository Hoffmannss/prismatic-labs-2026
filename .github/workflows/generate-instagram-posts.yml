name: 🤖 Gerar Posts Instagram Automático

on:
  workflow_dispatch:
    inputs:
      mes:
        description: 'Mês para gerar posts (ex: Fevereiro)'
        required: true
        default: 'Fevereiro'
  schedule:
    - cron: '0 23 25 * *'

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Instalar dependências
        run: |
          cd automation/instagram
          npm install

      - name: 🎯 Gerar tópicos
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          cd automation/instagram
          node scripts/1-generate-topics.js "${{ github.event.inputs.mes || 'Auto' }}"

      - name: 🎨 Criar HTMLs
        run: |
          cd automation/instagram
          node scripts/2-create-html.js

      - name: 📸 Gerar screenshots
        run: |
          cd automation/instagram
          node scripts/3-screenshots.js

      - name: ✍️ Gerar legendas
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          cd automation/instagram
          node scripts/4-generate-captions.js

      - name: ☁️ Upload Google Drive
        env:
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
        run: |
          cd automation/instagram
          node scripts/5-upload-drive.js

      - name: 🚀 Trigger Make.com
        env:
          MAKE_WEBHOOK_URL: ${{ secrets.MAKE_WEBHOOK_URL }}
        run: |
          cd automation/instagram
          node scripts/6-trigger-make.js

      - name: ✅ Commit resultados
        run: |
          git config user.name "Instagram Bot"
          git config user.email "bot@prismatic-labs.com"
          git add automation/instagram/generated/
          git commit -m "📸 Posts gerados: ${{ github.event.inputs.mes || 'Auto' }}" || echo "Sem mudanças"
          git push || echo "Sem push necessário"